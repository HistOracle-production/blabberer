{
  "name": "blabber-demo",
  "version": "0.1.0",
  "private": true,
  "description": "Interactive lip sync demo — load sprites + audio for real-time mouth animation",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
